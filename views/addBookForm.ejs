<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles.css" />
    <title>Add a Book to Inventory</title>
  </head>
  <body>
    <%- include('navbar') %>
    <h2>Add a Book to Inventory</h2>
    <div class="form-div">
      <form action="/add" method="POST" class="book-form">
        <label for="title">Title</label>
        <input type="text" id="title" name="title" />
        <a href="/add-author">Add new Author</a>
        <label for="author-forename">Author Forename</label>
        <select name="forename" id="author-forename" required>
          <% for( let author of authors ) { %>
          <option value="<%=author.forename %>"><%=author.forename %></option>
          <% } %>
        </select>

        <label for="author-surname">Author Surname</label>
        <select name="surname" id="author-surname" required>
          <% for( let author of authors ) { %>
          <option value="<%=author.surname %>"><%=author.surname %></option>
          <% } %>
        </select>

        <label for="genre">Genre</label>
        <a href="/add-genre">Add new Genre</a>
        <select name="genre" id="genre" required>
          <% for( let genre of genres ) { %>
          <option value="<%=genre.name %>"><%=genre.name %></option>
          <% } %>
        </select>
        <label for="condition">Condition</label>
        <select name="condition" id="condition" required>
          <option value="New">New</option>
          <option value="Used - Good">Used - good</option>
          <option value="Used - Acceptable">Used - acceptable</option>
        </select>
        <label for="availability">Availability</label>
        <select name="availability" id="availability" required>
          <option value="true">Available</option>
          <option value="false">Unavailable</option>
        </select>
        <label for="url">Cover Image URL</label>
        <input type="url" id="url" name="cover_image_url" />
        <button type="submit">Add to Inventory</button>
      </form>
    </div>
  </body>
</html>
